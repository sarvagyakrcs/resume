/** @type {import('next').NextConfig} */
// const nextConfig = {};

const nextConfig = {
    images: {
      domains: [
        "img.clerk.com",
        "fastly.picsum.photos"
      ]
    }
  };

export default nextConfig;